import { View } from "react-native";
import { GlobalSizes } from "../styles/sizes";

export const SpacingContainerSmall = () => {
    return <View style={{ height: GlobalSizes.spacingSizeSmall }} />;
};
